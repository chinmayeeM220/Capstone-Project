aapiVersion: apps/v1
kind: Deployment
metadata:
  name: sentiment_analysis
  labels:
    app: sentiment_analysis
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sentiment_analysis
  template:
    metadata:
      labels:
        app: sentiment_analysis
    spec:
      containers:
      - name: sentiment_analysis
        image: 605893374911.dkr.ecr.us-east-1.amazonaws.com/sentiment_analysis:latest
        ports:
        - containerPort: 5000
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "1"
        env:
        - name: CAPSTONE_TEST
          valueFrom:
            secretKeyRef:
              name: capstone-secret
              key: CAPSTONE_TEST

---
apiVersion: v1
kind: Service
metadata:
  name: sentiment_analysis-service
spec:
  type: LoadBalancer
  selector:
    app: sentiment_analysis
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000